#ifndef SENSOR_H
#define SENSOR_H

#include <QByteArray>
#include <QTextStream>
#include <vector>

class sensor{
    public:
        double time;
        std::vector<double> data;

    private:
        double getAverage(getAverage){

        };
};

#endif // SENSOR_H
